import 'package:flutter/material.dart';

class Statwidg extends StatelessWidget {
  const Statwidg({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("hello"),
    );
  }
}
